name: Workflow run

on:
  workflow_run:
    workflows: [ events ]
    types:
        - completed
jobs:
    echo-string:
        runs-on: ubuntu-latest
        steps:
            - run: echo "I was triggred because 'ebvets' workflow completed"        
